import React from 'react'

export default function ChatBox({ auctionId }) {
  return (
    <div>
      <p><em>ChatBox placeholder for auction {auctionId} — chat integration already exists on backend; wire WebSocket here.</em></p>
    </div>
  )
}
