import React from 'react'

export default function LoginForm() {
  return (
    <div>
      <p><em>LoginForm placeholder — wire REST or socket-based auth here.</em></p>
      <input placeholder="username" />
      <input type="password" placeholder="password" />
      <button>Login</button>
    </div>
  )
}
